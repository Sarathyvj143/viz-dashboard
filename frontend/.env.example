# API Configuration
VITE_API_URL=http://localhost:8000/api

# Development Configuration
# Default workspace ID for development mode
# In production, this should not be set - users must register and create workspace
VITE_DEV_DEFAULT_WORKSPACE_ID=1

# Mock Data Configuration
# Set to 'true' to use mock data for virtual datasets (development)
# Set to 'false' to use real backend API (production)
VITE_MOCK_VIRTUAL_DATASETS=true
